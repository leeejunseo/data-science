# 🚀 6DOF 탄도미사일 시뮬레이션 및 ML 기반 탄종 분류 시스템

> **데이터과학 학기 프로젝트**  
> Quaternion 기반 True 6DOF 비행역학 시뮬레이션과 머신러닝을 활용한 탄도미사일 자동 식별 시스템

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [핵심 기술](#2-핵심-기술)
3. [시스템 아키텍처](#3-시스템-아키텍처)
4. [6DOF 시뮬레이션 엔진](#4-6dof-시뮬레이션-엔진)
5. [미사일 모델](#5-미사일-모델)
6. [시그니처 특성 추출](#6-시그니처-특성-추출)
7. [머신러닝 파이프라인](#7-머신러닝-파이프라인)
8. [실행 방법](#8-실행-방법)
9. [실험 결과](#9-실험-결과)
10. [프로젝트 구조](#10-프로젝트-구조)

---

## 1. 프로젝트 개요

### 1.1 연구 배경

현대 탄도미사일은 다양한 비행 특성을 가지며, 레이더로 탐지된 궤적 데이터만으로 미사일 종류를 신속하게 식별하는 것은 방공 체계의 핵심 과제입니다. 본 프로젝트는 **물리 기반 시뮬레이션**과 **데이터 기반 머신러닝**을 결합하여 탄도미사일 자동 분류 시스템을 구현합니다.

### 1.2 연구 목표

1. **정밀한 6자유도(6DOF) 탄도미사일 시뮬레이터 구현**
   - Quaternion 기반 자세 표현 (Gimbal Lock 문제 해결)
   - 실제 물리 파라미터 기반 비행역학 모델링
   
2. **레이더 관측 가능한 시그니처 특성 설계**
   - 32개 → 15개 특성으로 정제 (레이더 관측 현실성 반영)
   - 6DOF 고유 동역학 특성 포함

3. **머신러닝 기반 탄종 분류 모델 개발**
   - RandomForest 분류기 활용
   - 발사각 그룹 단위 교차검증 (데이터 누수 방지)

### 1.3 대상 미사일

| 미사일 | 국가 | 사거리 | 특징 |
|--------|------|--------|------|
| **SCUD-B** | 구소련 | 300 km | 액체추진, 표준 탄도 |
| **Nodong** | 북한 | 1,500 km | 액체추진, 중거리 |
| **KN-23** | 북한 | 600~800 km | 고체추진, **편평 탄도(Depressed Trajectory)** |

---

## 2. 핵심 기술

### 2.1 물리 시뮬레이션

```
┌─────────────────────────────────────────────────────────────┐
│                    True 6DOF Equations                       │
├─────────────────────────────────────────────────────────────┤
│  State Vector (13D):                                         │
│  [x, y, z, u, v, w, q₀, q₁, q₂, q₃, p, q, r]               │
│                                                              │
│  • Position (x,y,z): Inertial NED frame [m]                 │
│  • Body Velocity (u,v,w): Body frame [m/s]                  │
│  • Quaternion (q₀~q₃): Attitude (scalar-first)              │
│  • Angular Rates (p,q,r): Body frame [rad/s]                │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 참조 문헌

- Zipfel, "Modeling and Simulation of Aerospace Vehicle Dynamics"
- Stevens & Lewis, "Aircraft Control and Simulation"
- MIL-HDBK-1211, "Missile Flight Simulation"

### 2.3 기술 스택

| 분류 | 기술 |
|------|------|
| 언어 | Python 3.10+ |
| 수치 계산 | NumPy, SciPy (`solve_ivp`) |
| 머신러닝 | scikit-learn (RandomForest) |
| 시각화 | Matplotlib (TkAgg) |
| 데이터 저장 | NPZ (압축 NumPy) |

---

## 3. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                        전체 시스템 구조                               │
└─────────────────────────────────────────────────────────────────────┘

  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
  │   미사일      │     │   6DOF       │     │   궤적       │
  │   파라미터    │────▶│   시뮬레이터  │────▶│   데이터     │
  │ (config_6dof)│     │(missile_6dof │     │   (NPZ)      │
  └──────────────┘     │  _true.py)   │     └──────┬───────┘
                       └──────────────┘            │
                                                   ▼
  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
  │   학습된      │     │   ML 학습    │     │   시그니처   │
  │   분류 모델   │◀────│  파이프라인   │◀────│   추출       │
  │ (RandomForest)│     │(train_large) │     │(signature_  │
  └──────┬───────┘     └──────────────┘     │ generator)  │
         │                                   └──────────────┘
         ▼
  ┌──────────────┐     ┌──────────────┐
  │   실시간      │     │   시각화     │
  │   탄종 분류   │────▶│   & 게임     │
  │(game_launcher)│     │              │
  └──────────────┘     └──────────────┘
```

---

## 4. 6DOF 시뮬레이션 엔진

### 4.1 핵심 파일: `missile_6dof_true.py`

**True6DOFSimulator** 클래스는 정통 6자유도 운동방정식을 구현합니다.

#### Quaternion 기반 자세 표현

```python
def quat_to_dcm(q: np.ndarray) -> np.ndarray:
    """
    Quaternion → Direction Cosine Matrix (Body-to-Inertial NED)
    
    좌표계 규약:
    - Body: x-forward, y-right, z-down
    - Inertial NED: x-north, y-east, z-down
    
    Reference: Stevens & Lewis Eq. 1.3-24
    """
```

#### 운동방정식

```python
def equations_of_motion(self, t, state):
    """
    6DOF 운동방정식 (13-state)
    
    힘/모멘트:
    - 중력 (Gravity)
    - 추력 (Thrust) + TVC 제어 모멘트
    - 공력 (Aerodynamic Forces & Moments)
    
    적분기: scipy.integrate.solve_ivp (RK45)
    """
```

### 4.2 대기 모델: ISA 1976

```python
def get_atmosphere(h: float) -> Tuple[float, float, float, float]:
    """
    ISA 1976 Standard Atmosphere
    
    Returns: (density, pressure, temperature, sound_speed)
    
    고도 구간:
    - 0~11km: 대류권 (온도 감률 -6.5°C/km)
    - 11~25km: 성층권 (등온)
    - 25km+: 상부 대기
    """
```

### 4.3 KN-23 편평 탄도: `kn23_depressed.py`

KN-23은 일반 탄도미사일과 달리 **편평 탄도(Depressed Trajectory)**를 사용합니다.

```
일반 탄도 vs 편평 탄도:

        일반 탄도                    편평 탄도 (KN-23)
         *                                
        / \                           ___________
       /   \                         /           \
      /     \                       /  Max Alt    \
     /       \                     /   40~60km     \
    /         \                   /                 \  ← Pull-up 기동
   /           \                 /                   \
  ●─────────────●               ●─────────────────────●
  Launch    Impact              Launch              Impact
  
  Max Alt: 150~300km            Max Alt: 40~60km
```

**특징:**
- 낮은 최대 고도 (40~60km)
- 종말 단계 Pull-up 기동
- 요격 회피에 유리

---

## 5. 미사일 모델

### 5.1 설정 파일: `config_6dof.py`

각 미사일의 물리적 파라미터를 정의합니다.

| 파라미터 | SCUD-B | Nodong | KN-23 |
|----------|--------|--------|-------|
| **총 질량** | 5,900 kg | 16,250 kg | 3,415 kg |
| **추진제 질량** | 3,800 kg | 12,150 kg | 2,500 kg |
| **직경** | 0.88 m | 1.36 m | 0.95 m |
| **길이** | 10.94 m | 16.4 m | 7.5 m |
| **연소 시간** | 65 s | 95 s | 50 s |
| **Isp (해면)** | 226 s | 250 s | 260 s |
| **Isp (진공)** | 254 s | 276 s | 275 s |
| **추진제** | 액체 (UDMH) | 액체 (UDMH) | 고체 |

### 5.2 공력 미분계수

```python
# 정적 안정성 (Static Stability)
C_m_alpha = -2.0    # 피치 모멘트 기울기 [/rad]

# 동적 안정성 (Dynamic Stability)  
C_mq = -150.0       # 피치 댐핑 [/rad]
C_nr = -150.0       # 요 댐핑 [/rad]
C_lp = -30.0        # 롤 댐핑 [/rad]

# 양력 기울기
CL_alpha = 3.5      # [/rad]
```

### 5.3 항력 계수 (Mach 의존)

```python
MISSILE_CD_TABLES = {
    'SCUD-B': {
        'mach': [0.0, 0.5, 0.8, 1.0, 1.2, 1.5, 2.0, 3.0, 4.0, 5.0],
        'cd':   [0.32, 0.36, 0.47, 0.82, 0.62, 0.42, 0.37, 0.32, 0.30, 0.27]
    },
    # ... (천음속 영역에서 항력 급증)
}
```

---

## 6. 시그니처 특성 추출

### 6.1 설계 철학

**"레이더로 관측 가능한 특성만 사용"**

초기 32개 특성에서 레이더 관측 불가능한 특성(설계상수, 내부 파라미터)을 제거하고, 6DOF 기동성 특성을 추가하여 **15개 특성**으로 정제했습니다.

### 6.2 15차원 시그니처 특성

#### 궤적 형태 (4개) - 레이더 추적 관측

| 특성 | 설명 | 단위 |
|------|------|------|
| `max_altitude_km` | 최대 고도 | km |
| `final_range_km` | 최종 사거리 | km |
| `impact_angle_deg` | 낙하각 | deg |
| `total_flight_time` | 총 비행시간 | s |

#### 속도/마하 (4개) - 레이더 도플러 관측

| 특성 | 설명 | 단위 |
|------|------|------|
| `max_velocity` | 최대 속도 | m/s |
| `terminal_velocity` | 종말 속도 | m/s |
| `max_mach` | 최대 마하수 | - |
| `velocity_loss_ratio` | 속도 손실률 | - |

#### 감속/기동/효율 (4개) - 궤적 변화 추정

| 특성 | 설명 | 단위 |
|------|------|------|
| `max_deceleration` | 최대 감속도 | m/s² |
| `ground_track_curvature` | 지상 궤적 곡률 | 1/m |
| `path_efficiency` | 경로 효율성 | - |
| `energy_ratio` | 에너지 비율 (KE/PE) | - |

#### 6DOF 기동성 보강 (3개) - 동적 특성

| 특성 | 설명 | 의미 |
|------|------|------|
| `alpha_std_deg` | 받음각 표준편차 | 기동 강도 |
| `q_max_deg_s` | 최대 피치율 | 급기동 지표 |
| `alpha_q_correlation` | α-q 상관계수 | 동적 안정성 |

### 6.3 특성 추출 코드

```python
# signature_generator.py

def _extract_signature_features(self, results, missile_type, launch_angle):
    """
    6DOF 시뮬레이션 결과에서 15차원 시그니처 추출
    """
    features = np.zeros(15, dtype=np.float32)
    
    # 궤적 형태
    features[0] = np.max(h) / 1000  # max_altitude_km
    features[1] = np.sqrt(x[-1]**2 + y[-1]**2) / 1000  # final_range_km
    features[2] = np.abs(np.rad2deg(gamma[-1]))  # impact_angle_deg
    features[3] = t[-1]  # total_flight_time
    
    # 속도/마하
    features[4] = np.max(V)  # max_velocity
    features[5] = V[-1]  # terminal_velocity
    features[6] = np.max(mach)  # max_mach
    features[7] = (np.max(V) - V[-1]) / (np.max(V) + 1e-6)  # velocity_loss_ratio
    
    # 감속/기동/효율
    features[8] = np.min(np.gradient(V, t))  # max_deceleration
    features[9] = np.mean(curvature)  # ground_track_curvature
    features[10] = final_range / path_length  # path_efficiency
    features[11] = KE / PE  # energy_ratio
    
    # 6DOF 기동성
    features[12] = np.std(np.rad2deg(alpha))  # alpha_std_deg
    features[13] = np.max(np.abs(np.rad2deg(q)))  # q_max_deg_s
    features[14] = np.corrcoef(alpha, q)[0,1]  # alpha_q_correlation
    
    return features
```

---

## 7. 머신러닝 파이프라인

### 7.1 데이터 생성

```bash
python train_large.py
```

**생성 설정:**
- 미사일: SCUD-B, Nodong, KN-23 (3종)
- 발사각: 15°~80°, 2° 간격 (33개)
- 샘플/각도: 50개
- 노이즈: ±2° (발사각 변동)
- **총 샘플: 4,950개** (3 × 33 × 50)

### 7.2 모델 학습 및 평가

```bash
python eval_by_angle.py
```

#### GroupShuffleSplit (발사각 그룹 단위 분할)

**문제:** 일반적인 랜덤 분할은 같은 발사각의 샘플이 train/test에 모두 포함되어 **데이터 누수** 발생

**해결:** 발사각(nominal_angle)을 **그룹**으로 지정하여 완전 분리

```python
from sklearn.model_selection import GroupShuffleSplit

gss = GroupShuffleSplit(n_splits=1, test_size=0.2, random_state=42)
train_idx, test_idx = next(gss.split(X, y, groups=nominal_angles))

# 결과: train/test 발사각 완전 분리
# Train: [15, 17, 19, 21, 23, 25, 27, 29, 35, 37, ...]°
# Test:  [31, 33, 45, 49, 53, 67, 77]°
```

### 7.3 분류 모델

```python
model = RandomForestClassifier(
    n_estimators=100,
    max_depth=15,
    min_samples_split=5,
    min_samples_leaf=2,
    random_state=42,
    n_jobs=-1
)
```

### 7.4 저장된 모델

| 파일 | 설명 |
|------|------|
| `trained_models/rf_model.pkl` | 학습된 RandomForest |
| `trained_models/scaler.pkl` | StandardScaler |
| `trained_models/feature_names.pkl` | 15개 특성명 |
| `trained_models/missile_types.pkl` | 3개 탄종명 |

---

## 8. 실행 방법

### 8.1 환경 설정

```bash
cd final
pip install -r requirements.txt
```

### 8.2 전체 파이프라인 실행

```bash
# 1. 대량 데이터 생성 + 학습
python train_large.py

# 2. 발사각 그룹 평가 + 모델 저장
python eval_by_angle.py

# 3. 게임 서버 실행
python game_launcher.py
```

### 8.3 개별 시뮬레이션

```bash
# 시각화 (12-subplot 그래프)
python main_visualization.py

# NPZ 파일 분석
python view_npz.py results_6dof/SCUD-B_45.0deg_20251219_220432.npz
```

### 8.4 게임 모드

```bash
# 데모 모드 (단일 분류)
python game_launcher.py --demo

# API 서버 모드 (웹 연동)
python game_launcher.py --port 5000
```

**API 엔드포인트:**
| 엔드포인트 | 설명 |
|------------|------|
| `GET /api/start` | 새 게임 시작 (랜덤 미사일) |
| `GET /api/analyze` | 시그니처 분석 실행 |
| `GET /api/reveal` | 정답 공개 |
| `POST /api/guess` | 사용자 추측 제출 |

---

## 9. 실험 결과

### 9.1 분류 성능

```
======================================================================
📊 분류 결과 (발사각 그룹 평가)
======================================================================

✅ 정확도 (Accuracy): 100.00%

📋 Classification Report:
              precision    recall  f1-score   support
      SCUD-B       1.00      1.00      1.00       100
      Nodong       1.00      1.00      1.00       100
       KN-23       1.00      1.00      1.00       100

📊 Confusion Matrix:
                SCUD-B    Nodong     KN-23
      SCUD-B       100         0         0
      Nodong         0       100         0
       KN-23         0         0       100
```

### 9.2 중요 특성 (Feature Importance)

```
🔍 중요 시그니처 TOP 10:

순위  Feature Name              Importance
─────────────────────────────────────────
  1.  max_mach                    15.9%
  2.  final_range_km              15.3%
  3.  max_velocity                14.6%
  4.  q_max_deg_s                 10.4%  ← 6DOF 특성!
  5.  path_efficiency             10.2%
  6.  terminal_velocity            8.1%
  7.  alpha_std_deg                6.2%  ← 6DOF 특성!
  8.  max_altitude_km              5.5%
  9.  alpha_q_correlation          4.3%  ← 6DOF 특성!
 10.  total_flight_time            3.0%
```

### 9.3 결과 해석

1. **최대 마하수, 사거리, 최대속도**가 가장 중요한 분류 기준
2. **6DOF 기동성 특성** (`q_max_deg_s`, `alpha_std_deg`)이 상위 10위 내 진입
3. **100% 정확도** 달성 (발사각 그룹 분리 평가에서도)

---

## 10. 프로젝트 구조

```
final/
│
├── 📄 README.md                    # 프로젝트 문서 (현재 파일)
├── 📄 requirements.txt             # Python 의존성
│
├── 🔧 설정 파일
│   ├── config.py                   # 기본 설정
│   └── config_6dof.py              # 6DOF 미사일 파라미터 ★
│
├── 🚀 시뮬레이션 엔진
│   ├── missile_6dof_true.py        # True 6DOF 시뮬레이터 (Quaternion) ★★★
│   └── kn23_depressed.py           # KN-23 편평 탄도 시뮬레이터 ★★
│
├── 📊 데이터 처리
│   ├── trajectory_io.py            # NPZ 저장/로드 표준 모듈
│   ├── signature_generator.py      # 시그니처 추출 (15차원) ★★
│   └── signature_extractor.py      # 시그니처 추출 (구버전)
│
├── 🤖 머신러닝
│   ├── train_large.py              # 대량 학습 파이프라인 ★★
│   └── eval_by_angle.py            # 발사각 그룹 평가 + 모델 저장 ★★★
│
├── 🎮 게임/시각화
│   ├── game_launcher.py            # 게임 서버 (ML 분류 통합) ★★
│   ├── missile_manager.py          # 미사일 관리 API
│   ├── main_visualization.py       # 12-subplot 시각화
│   └── view_npz.py                 # NPZ 파일 뷰어
│
├── 📁 데이터 폴더
│   ├── results_6dof/               # 시뮬레이션 결과 (NPZ, PNG)
│   ├── signature_dataset/          # 학습 데이터셋
│   │   └── dataset_large.npz       # 4,950 샘플, 15 특성
│   └── trained_models/             # 학습된 ML 모델 ★
│       ├── rf_model.pkl            # RandomForest 모델
│       ├── scaler.pkl              # StandardScaler
│       ├── feature_names.pkl       # 특성명 리스트
│       └── missile_types.pkl       # 탄종명 리스트
│
└── 🧪 테스트
    ├── test_6dof_fix.py
    ├── test_kn23_angles.py
    └── test_signature_extractor.py
```

---

## 📚 참고 자료

### 학술 문헌
- Zipfel, P. H. (2007). *Modeling and Simulation of Aerospace Vehicle Dynamics*. AIAA.
- Stevens, B. L., & Lewis, F. L. (2003). *Aircraft Control and Simulation*. Wiley.
- MIL-HDBK-1211 (1995). *Missile Flight Simulation*. US DoD.

### 미사일 데이터
- CSIS Missile Threat: https://missilethreat.csis.org/
- 38 North: https://www.38north.org/

---

## 📝 라이선스

본 프로젝트는 교육 목적으로 작성되었습니다.

---

*Last updated: 2024-12*
